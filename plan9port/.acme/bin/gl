#!/bin/sh
# show git-log for given or current file in new Acme window
if [ -z "$samfile" ]; then 9 echo 'Must be used from Acme or Sam' 1>&2; exit 1; fi
file=${1:-$samfile}
git log --oneline -- "$file" | plumb -i -d edit -a "action=showdata filename=$file+Log"
