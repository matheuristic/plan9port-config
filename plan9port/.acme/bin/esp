#!/usr/bin/env rc

# Spellchecks argument file using the enchant command-line tool

if (! enchant -h >[2]/dev/null >[1=2]) {
	echo enchant not installed
	exit 1
}

if (test $#* -eq 0 || test $#* -gt 1) {
	echo usage: $0 FILENAME
	exit 1
}

enchant -L -l $1 |  9 awk '{print "'^$1^'" ":" $0}'
