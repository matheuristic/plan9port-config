# Tag
Edit Font (Indent on) <cl-paste |c- |c+ |is- |is+ |t2s |dtw d rg+
|ct |lower |upper |surround |s2t |uline alink gl gb sp lw :, :0 :$
[F GoRegular 22a] F- F+ Slide Slide- Slide+ :/[Ffoo]/ :-/[Ff]oo/ :42

# Edit commands
=                   # line number
.,3                 # select from cursor to end of line 3
3,.                 # select from beginning of line 3 to cursor
+/text/             # search forward for text
$-/text/            # search backward from end for text
,d                  # clear whole buffer window, useful for +Error
,>wc                # line, word and char counts for whole buffer
s/[Ff]rom/to/g      # regexp replace across selected text
,s/[Ff]rom/to/g     # regexp replace across all text in current window
X                   # list all windows
X/^'/               # list all modified files
X/^'/ w             # write all modified files

# Characters
αβγδεζηθλμνξπρστφψωΓΔΘΛΞΠΣΦΨΩ   # Greek
–—‘’“”…«»•©®™†‡°·§¶№¡¿◊¢£€ÆŒæœ  # Typography
±×÷√∛∑∏∫≠≈≤≥∂∆∇∞∩¬              # Math
←→↑↓¹²³ⁱⁿₖₘₙ                    # Other
÷πℯ∈∉∋∌∘√∛∩∪≈≉≠≡≢≤≥⊆⊇⊈⊉⊊⊋⊻⊼⊽    # Julia math constants and operators

# Other ~/.acme/bin commands
ci text             # gen case-insensitive regexp for non-regexp text
csp '**/*.py'       # `cspell` code spell-check for Python files

# Git
git grep -n 'some[Pp]attern'
git log --oneline -10
git status
git diff
git difftool --dir-diff
git commit -a
git push -u origin/HEAD
git fetch
git merge origin/HEAD
git mergetool
git rebase -i HEAD~3     # NOTE: oldest commit must be set to "pick"
git checkout BRANCH
git checkout -b BRANCH
git bisect         # start > bad > good COMMIT > (bad|good)* > reset

# Watch (requires 9fans.net/go/acme/Watch Go pkg)
Watch [-r] COMMAND [ARG1] ...

# LSP (requires github.com/fhs/acme-lsp/cmd/{acme-lsp,L} Go pkgs)
win acme-lsp
Lcomp Ldef Lfmt Lhov Limpls Lrefs [Lrn SYM] Lsig Lsyns Ltype Lws+ Lws-

# DevSkim (https://github.com/microsoft/DevSkim)
devskim analyze -f text -o '%F:%L:%C,%l:%c (%S) [%R] %N' $%

# ShellCheck (https://github.com/koalaman/shellcheck)
shellcheck -a -f gcc $%

# mdformat (https://github.com/executablebooks/mdformat)
Edit ,|mdformat --number --wrap 70 -

# Go tools gofmt and goimports
Edit ,|gofmt
Edit ,|goimports
Edit ,|goreturns

# Pandoc (https://pandoc.org/)
pandoc -o /tmp/tmp.html FILENAME.md && { xdg-open /tmp/tmp.html || open /tmp/tmp.html } &

# Python Black (https://github.com/psf/black)
Edit ,|black -q -

# Pylint (https://github.com/PyCQA/pylint)
condarun ENVNAME pylint -f parseable $%

# Plumber rules
cat $home/lib/plumbing | 9p write plumb/rules

# Advanced Edit commands
# - Find occurrences of 'User' or 'user' in window, with context
Edit , x g/[Uu]ser/ {
	=
	-1,+1p
}
# - Replace all occurrences of 'From' or 'from' to 'to' in all windows
Edit X/^[^\+]./ {
	, s/[Ffrom]/to/g
}
